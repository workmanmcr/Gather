@{
    Layout = "_Layout";
}

@using Gather.Models

<h1>Activities</h1>

@if (@Model.Count == 0)
{
    <h3> No Gatherings have been Planned yet. </h3>
}
else
{
    <ul>
        @foreach (Gathering g in Model)
        {
            <li>@Html.ActionLink($"{g.Title}", "Details", new { id = g.GatheringId })</li>
        }
    </ul>
}